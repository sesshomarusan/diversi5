local gateKeysInHand = 0
local frontGate = script:FindDescendantByName("Front Gate")
local bossGate = script:FindDescendantByName("Boss Gate")


function Tick()
	if gateKeysInHand == 1 then
		-- Open gate to boss
		-- if front gate is not open, then open it
	end
	if gateKeysInHand == 6 then
		-- Open gate to boss
		-- if boss gate is not open, then open it
	end
	